'use strict';

/**
 * policy-privacy controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::policy-privacy.policy-privacy');
